node {
    stage 'Stage 1 - scm'
    echo 'Stage 1 - scm'
    checkout scm
    stage 'Stage test'
    echo 'stuff'
    sh 'touch TEST-*.xml'
    step([$class: 'JUnitResultArchiver', testResults: 'TEST-*.xml'])
    
}